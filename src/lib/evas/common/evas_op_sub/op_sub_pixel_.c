/* sub pixel --> dst */

/**
 * @brief Initializes C-specific functions for sub-pixel span operations.
 *
 * This function is intended to set up function pointers or perform other
 * initialization for operations that involve rendering spans of pixels
 * with sub-pixel accuracy, where the source is a sub-pixel value and
 * the destination is a pixel.
 */
static void
init_sub_pixel_span_funcs_c(void)
{
}

/**
 * @brief Initializes C-specific functions for sub-pixel point operations.
 *
 * This function is intended to set up function pointers or perform other
 * initialization for operations that involve rendering individual points
 * with sub-pixel accuracy, where the source is a sub-pixel value and
 * the destination is a pixel.
 */
static void
init_sub_pixel_pt_funcs_c(void)
{
}

/*-----*/

/* sub_rel pixel --> dst */

/**
 * @brief Initializes C-specific functions for sub-relative pixel span operations.
 *
 * This function is intended to set up function pointers or perform other
 * initialization for operations that involve rendering spans of pixels
 * where the source is a sub-pixel value relative to a pixel grid, and
 * the destination is a pixel.
 */
static void
init_sub_rel_pixel_span_funcs_c(void)
{
}

/**
 * @brief Initializes C-specific functions for sub-relative pixel point operations.
 *
 * This function is intended to set up function pointers or perform other
 * initialization for operations that involve rendering individual points
 * where the source is a sub-pixel value relative to a pixel grid, and
 * the destination is a pixel.
 */
static void
init_sub_rel_pixel_pt_funcs_c(void)
{
}
