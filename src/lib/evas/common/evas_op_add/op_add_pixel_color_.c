/* add pixel x color --> dst */

/**
 * @brief Initializes span-based functions for adding pixel and color.
 *
 * This function sets up the necessary function pointers or configurations
 * for operations that process a horizontal span of pixels, combining
 * each source pixel with a given color and writing to a destination.
 */
static void
init_add_pixel_color_span_funcs_c(void)
{
}

/**
 * @brief Initializes point-based functions for adding pixel and color.
 *
 * This function sets up the necessary function pointers or configurations
 * for operations that process a single pixel, combining the source pixel
 * with a given color and writing to a destination.
 */
static void
init_add_pixel_color_pt_funcs_c(void)
{
}

/*-----*/

/* add_rel pixel x color --> dst */

/**
 * @brief Initializes span-based functions for relative addition of pixel and color.
 *
 * This function sets up the necessary function pointers or configurations
 * for operations that process a horizontal span of pixels, performing a
 * relative addition of each source pixel with a given color and writing
 * to a destination. "Relative" typically implies adding to the existing
 * destination pixel value.
 */
static void
init_add_rel_pixel_color_span_funcs_c(void)
{
}

/**
 * @brief Initializes point-based functions for relative addition of pixel and color.
 *
 * This function sets up the necessary function pointers or configurations
 * for operations that process a single pixel, performing a relative addition
 * of the source pixel with a given color and writing to a destination.
 * "Relative" typically implies adding to the existing destination pixel value.
 */
static void
init_add_rel_pixel_color_pt_funcs_c(void)
{
}
